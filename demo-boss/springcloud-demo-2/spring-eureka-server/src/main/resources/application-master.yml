server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: true
spring:
  application:
    name: eurekaserver
