<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mybatis.dao.DepartmentMapper">

        <!-- Department getDeptById(Integer id);   -->
        <select id="getDeptById" resultType="com.atguigu.mybatis.bean.Department">
                select * from tbl_dept where id=#{id}
        </select>

        <!--collection 嵌套结果集的方式,定义关联的集合元素的封装规则-->
        <!--
           did dept_nam eid last_name email gender
        -->
        <resultMap id="MyDept" type="com.atguigu.mybatis.bean.Department">
                <id column="did" property="id"/>
                <result column="dept_name" property="deptName"/>
                <!--collection定义关联集合类型的封装规则
                     ofType:指定集合里面元素的类型
                -->
                <collection property="emps" ofType="com.atguigu.mybatis.bean.Employee">
                        <!--定义这个集合中元素的封装规则-->
                        <id column="eid" property="id"/>
                        <result column="last_name" property="lastName"/>
                        <result column="email" property="email"/>
                        <result column="gender" property="gender"/>
                </collection>

        </resultMap>

        <!--Department getDeptWithEmpsById(Integer id);-->
        <select id="getDeptWithEmpsById" resultMap="MyDept">
                select d.id did,d.dept_name dept_name,e.id eid,e.last_name last_name,e.email email,e.gender gender
                from tbl_dept d
                left join tbl_employee e
                on d.id=e.d_id
                where d.id=#{id}
        </select>



        <!--Department getDeptByIdStep(Integer id);-->
        <resultMap id="MyDeptStep" type="com.atguigu.mybatis.bean.Department">
                <id column="id" property="id"/>
                <result column="dept_name" property="deptName"/>
                <collection property="emps"
                            ofType="com.atguigu.mybatis.bean.Employee"
                            select="com.atguigu.mybatis.dao.EmployeeMapperPlus.getEmpsByDeptId"
                            column="{deptId=id}"
                            fetchType="lazy"/>



        </resultMap>

        <select id="getDeptByIdStep" resultMap="MyDeptStep">
                select * from tbl_dept where id=#{id}
        </select>


        <!--扩展：多列的值传递过去
                将多列的值封装map传递
                column="{key1=column1,key2=column2}" 其中key是参数名，column是对应列名
                fetchType="lazy":表示使用延迟加载
                          -lazy:延迟加载 (默认)
                          -eager:立即加载
        -->
</mapper>   