spring:
  application:
    name: zuul-service
#  redis:
#    database: 0
#    host: 112.124.21.230
#    password: zyz0302..
#    port: 23456
#    #jedis
#    lettuce:
#      pool:
#        max-active: 30
#        max-wait: 10000
#        max-idle: 30
#        min-idle: 10
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 9000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
zuul:
  routes:
    invoke:
      path: /**
      serviceId: ddo-invoke

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh